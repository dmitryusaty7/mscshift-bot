# MSCShift Bot

Телеграм-бот для управления сменами и отчётами. Проект собирается по блокам. Реализованы блоки 0–3 (точка входа, регистрация, основная панель, меню смены) и пользовательские сценарии блоков 4, 5, 6, 7.

## Технологический стек
- Node.js 22 (LTS)
- PostgreSQL
- Directus (только просмотр данных и файлов)
- node-telegram-bot-api
- Все записи пишутся напрямую в PostgreSQL (без Directus), типы и названия колонок соответствуют `db/mscshiftbot_schema.sql` (bigint идентификаторы, флаги `is_*`, датированные поля с `DEFAULT now()`).

## Архитектурные принципы
- Модульность: один файл — одна ответственность
- Все тексты вынесены в единый модуль `src/messages`
- Подключение к БД и репозитории разделены
- Простые логгеры и middleware для будущего расширения

## Структура проекта
```
src/
  bot/
    index.js              // запуск и регистрация обработчиков
    handlers/
      start.handler.js    // логика /start (Блоки 0-1)
    middlewares/
      session.js          // временное хранение состояния пользователя
  db/
    index.js              // создание пула PostgreSQL
    brigadiers.repo.js    // запросы к таблице brigadiers
    ships.repo.js         // запросы к таблице ships
    shifts.repo.js        // запросы к таблице shifts
    expenses.repo.js      // запросы к таблице shift_expenses
    holds.repo.js         // запросы к таблице holds
  directus/               // клиент Directus для работы с файлами и сущностями
  messages/
    index.js              // все тексты бота
  modules/
    register.js           // FSM регистрации нового пользователя (Блок 1)
    main-panel.js         // основная панель бригадира (Блок 2)
    shift-menu.js         // создание смены и меню смены (Блок 3)
    crew.js               // управление составом бригады (Блок 4)
    salary.js             // управление начислениями (Блок 5)
    materials.js          // фиксация материалов (Блок 6)
    expenses.js           // учёт расходов (Блок 7)
  config/
    env.js                // проверка окружения
  utils/
    logger.js             // простой логгер
    time.js               // форматирование дат
  index.js               // точка входа
```

## Блоки
- **Блок 0** — реализован: команда `/start`, поиск бригадира в БД, маршрутизация к регистрации или основной панели.
- **Блок 1** — реализован: последовательный сбор имени и фамилии, валидация кириллицы, запись в `brigadiers` со статусом `active`.
- **Блок 2** — реализован: основная панель бригадира с подсчётом активных смен и кнопками для навигации.
- **Блок 3** — реализован: создание смены, проверка дубликатов, транзакционное добавление трюмов и вывод меню смены.
- **Блок 4** — реализован: заполнение состава бригады (водитель, заместитель, рабочие) с прямыми запросами к PostgreSQL.
- **Блок 5** — реализован: ввод выплат по ролям с пересчётом итогов через PostgreSQL.
- **Загрузка фото** — реализовано: получение фото от пользователя, отправка в Directus `/files`, создание записи в `shift_photos` с привязкой к смене и пользователю.
- **Блок 6** — реализован: учёт использованных материалов за смену с гарантированной записью в `shift_materials` и подтверждением заполнения.
- **Блок 7** — реализован: ввод расходов за смену с пересчётом общего итога и проверкой заполненности всех категорий.

## Блок 0 — точка входа
1. Пользователь отправляет `/start`.
2. Бот ищет запись в таблице `brigadiers` по `telegram_id`.
   - Если запись найдена — открывается основная панель (Блок 2).
   - Если записи нет — запускается регистрация (Блок 1).

## Блок 1 — регистрация бригадира
1. Если бригадир не найден, бот переводит пользователя в состояние регистрации и просит ввести **имя** кириллицей (2–50 символов, первая буква заглавная, допускаются пробел и дефис).
2. После успешного ввода имени бот просит ввести **фамилию** с теми же правилами валидации.
3. При ошибке на любом шаге бот повторно запрашивает корректные данные.
4. После получения имени и фамилии создаётся запись в `brigadiers` (`first_name`, `last_name`, `telegram_id`, `status = active`, `created_at`, `updated_at`).
5. Пользователь получает подтверждение и перенаправляется в основную панель (Блок 2).

**Валидация ФИО:** регэксп `^[А-ЯЁ][а-яё\\- ]{1,49}$` (первая буква заглавная, кириллица, 2–50 символов, допускаются пробел и дефис).

### Как проверить работу блока
- Перезапустить сервис, чтобы бот получил новый код: `systemctl restart mscshift-bot`
- Пройти регистрацию в Telegram, выполнив шаги выше
- Убедиться, что новая запись появилась в таблице `brigadiers` со статусом `active`
- Проверить обновлённые файлы в GitHub

## Блок 2 — основная панель
1. После успешного входа или регистрации бот выводит панель с приветствием, текущей датой и списком активных смен (`is_closed = false`). Формат строк: `ДД.ММ.ГГГГ — Судно (трюмов: N)`.
2. Reply-кнопки панели:
   - `🚀 Открыть новую смену` — запускает сценарий создания смены (Блок 3).
   - `🧾 Активные смены` — переключает клавиатуру: каждая открытая смена отображается отдельной кнопкой и даёт переход в меню смены (Блок 3). Внизу добавляется кнопка `⬅ Назад`, которая возвращает исходную клавиатуру панели.
3. Команда `/menu` повторно открывает панель. При отсутствии записи в `brigadiers` бот просит пройти регистрацию.

### Как проверить работу блока
- Отправить `/start` или `/menu` для зарегистрированного бригадира и убедиться, что в сообщении видны ФИО, текущая дата и список активных смен.
- Нажать `🧾 Активные смены`: клавиатура должна поменяться на список смен + `⬅ Назад`, сообщения при этом не дублируются.
- Выбрать смену из списка и проверить, что бот открывает меню смены (Блок 3) для нужной записи.
- Нажать `⬅ Назад` и убедиться, что возвращается исходная клавиатура главной панели.

## Блок 3 — меню смены
Блок отвечает за создание новой смены и вывод меню статусов по основным разделам отчётности.

1. Команда `/shift` или кнопка «Открыть новую смену» запускает сценарий. Бот проверяет, зарегистрирован ли бригадир.
2. Пользователь вводит дату в формате `ДД.MM.ГГГГ` или нажимает кнопку «Сегодня».
3. Бот просит указать название судна (буквы, цифры, пробел и дефис) и при необходимости создаёт запись в таблице `ships`.
4. Пользователь выбирает количество трюмов от 1 до 7.
5. Перед созданием смены выполняется проверка дубликата по бригадиру, судну и дате. При наличии дубликата бот возвращает пользователя к шагу выбора даты.
6. При отсутствии дубликатов создаётся запись в `shifts` (флаги `is_crew_filled`, `is_salary_filled`, `is_materials_filled`, `is_expenses_filled`, `is_photos_filled` и `is_closed` в начальном состоянии `false`) и связанные трюмы в таблице `holds` (номера от 1 до N) в одной транзакции. Идентификаторы берутся из `bigint`-последовательностей БД.
7. Пользователь получает главное меню смены с датой, ФИО бригадира, судном, количеством трюмов и статусами разделов (иконки ✅/✍). Внизу — inline-кнопки разделов и reply-кнопка «⬅ Назад в главное меню».
8. Кнопка завершения смены станет доступна после реализации блоков 4–8 и заполнения всех разделов. Текущие переходы помечены как TODO.

### Как проверить работу блока
- Перезапустить сервис `systemctl restart mscshift-bot`
- В Telegram отправить `/shift` или нажать кнопку «Открыть новую смену» и пройти сценарий ввода даты, судна и трюмов
- Убедиться, что в БД появились записи в `ships`, `shifts` и `holds` для введённых данных и что дубликаты не создаются
- Проверить обновлённые файлы в GitHub

## Блок 4 — состав бригады
Блок отвечает за сбор состава смены через FSM и прямые операции в PostgreSQL (`drivers`, `workers`, `shift_crew`, `shift_workers`, `shifts`). Все сообщения — на русском языке, комментарии в коде — также на русском.

1. Из меню смены (inline-кнопка «Состав бригады») открывается интро-экран с кнопкой «Начать». Пользователь остаётся в меню смены при нажатии «Назад».
2. Хаб состава показывает текущий статус с иконками ✍️/✅, ФИО водителя, заместителя и список рабочих. Кнопки хаба:
   - Редактировать заместителя (опционально, есть кнопка «Пропустить»)
   - Редактировать водителя (обязательно)
   - Добавить рабочего (многократно)
   - Удаление рабочих через inline-кнопки `❌ ФИО`
   - Обновить хаб и подтвердить состав (доступно, когда есть водитель и хотя бы один рабочий)
3. Валидация ФИО: только кириллица, минимум два слова (пример: «Иванов Иван»). Некорректный ввод приводит к повторному запросу.
4. Запись в БД:
   - `drivers`/`workers` — поиск по `LOWER(full_name)`, при отсутствии создаётся запись со статусом `active`
   - `shift_crew` — upsert водителя с уникальностью по `shift_id`, привязка заместителя при наличии водителя
   - `shift_workers` — добавление/удаление рабочих, защита от дубликатов по (`shift_id`, `worker_id`)
   - `shifts.is_crew_filled` — пересчёт после каждого изменения: `driver_id` есть и минимум один рабочий
5. Кнопка подтверждения возвращает в меню смены с обновлёнными статусами. Пока смена не закрыта, данные можно редактировать повторно.

### Как проверить работу блока
- Открыть меню смены и нажать «Состав бригады»
- Ввести водителя и хотя бы одного рабочего, опционально — заместителя
- Проверить, что в БД появились/обновились записи в `drivers`, `workers`, `shift_crew`, `shift_workers`, а у смены выставлен флаг `is_crew_filled = true`
- Вернуться в меню смены и убедиться, что статус раздела сменился на ✅

## Блок 6 — материалы смены
Блок отвечает за фиксацию фактически использованных материалов по смене (не по трюмам).

1. Из меню смены (inline-кнопка «Материалы») всегда открывается экран 6.1 с вступительным текстом и reply-кнопками «▶ Начать заполнение материалов» и «⬅ Назад» (другие экраны недоступны до старта).
2. При входе в блок выполняется запрос `INSERT ... ON CONFLICT DO NOTHING` для создания строки в `public.shift_materials` по `shift_id`, чтобы дальнейшие `UPDATE` не теряли данные.
3. Нажатие «Начать» открывает хаб (экран 6.2) с текущими значениями из БД и inline-кнопками для каждого материала. Все значения всегда подгружаются через `SELECT` из `public.shift_materials`.
4. Для каждой позиции открывается экран ввода с валидацией: рулоны/лента — целое число ≥ 0; трубки ПВХ — число ≥ 0 с двумя знаками после точки. После ввода выполняется `UPDATE public.shift_materials ... WHERE shift_id = $1` с проверкой `rowCount` и повтором через гарантирующую вставку при необходимости.
5. В хабе всегда доступна inline-кнопка «✅ Подтвердить», которая выставляет `shifts.is_materials_filled = true` и возвращает в меню смены. Reply-кнопки «⬅️ Назад» обновляют хаб, «🏠 В Меню смены» также подтверждает блок.
6. Все действия работают только при наличии `shift_id` в сессии материалов; при отсутствии бот сообщает «Сначала выберите смену» и возвращает пользователя в меню смены.

## Блок 7 — расходы смены
Блок отвечает за сбор расходов по четырём категориям с хранением всех значений в `public.shift_expenses` и пересчётом итогов в БД.

1. Из меню смены (inline-кнопка «🧾 Расходы») всегда открывается экран 7.1 с вступительным текстом и reply-кнопками «▶ Начать заполнение расходов» и «⬅ Назад».
2. При входе в блок гарантируется наличие строки в `shift_expenses` для текущей смены через `INSERT ... ON CONFLICT DO NOTHING` по `shift_id`; колоноки: `food_amount`, `materials_amount`, `taxi_amount`, `other_amount`, `other_comment`, `total_expenses`.
3. Нажатие «Начать» открывает хаб (экран 7.2) без дублирования значений в тексте: суммы отображаются на inline-кнопках категорий «Питание», «Расходники», «Такси», «Прочее». Отдельная reply-кнопка — «🏠 В Меню смены».
4. Каждая категория открывает экран ввода с валидацией: только цифры, целое число от 0 до 999999. После успешного ввода выполняется `UPDATE public.shift_expenses` с пересчётом `total_expenses` на стороне БД.
5. Для категории «Прочее» допускается комментарий отдельным сообщением сразу после ввода суммы; текст сохраняется в `other_comment` и повторно показывает хаб.
6. После каждого сохранения пересчитывается итог по колонкам `_amount`, а при выходе кнопкой «🏠 В Меню смены» у смены фиксируется `is_expenses_filled = true` в таблице `shifts`.
7. Возвраты работают по кнопкам: «⬅ Назад» из экранов ввода возвращает в хаб, «🏠 В Меню смены» — обратно в меню смены.
8. Текст хаба расходов всегда непустой (Telegram запрещает пустые сообщения), хаб редактируется по `hubMessageId` при возможности, в противном случае отправляется новое сообщение.
9. Переходы строго следуют цепочке: кнопка «🧾 Расходы» в меню смены → экран 7.1 → «▶ Начать заполнение расходов» → хаб (экран 7.2) с интерактивными суммами.
10. Для стабильности хаб отправляется через `ctx.telegram.editMessageText`/`ctx.telegram.sendMessage` с защитой от пустого текста и повторной привязкой `hubMessageId`.

Короткий сценарий использования: откройте нужную смену → нажмите «🧾 Расходы» → заполните суммы по категориям → при необходимости оставьте комментарий к «Прочее». Все значения и итог сохраняются в `shift_expenses` с колонками `food_amount`, `materials_amount`, `taxi_amount`, `other_amount`, `other_comment`, `total_expenses`.

## Запуск
1. Скопировать пример окружения и заполнить переменные:
   - `TELEGRAM_BOT_TOKEN`
   - `PG_HOST`, `PG_PORT`, `PG_DATABASE`, `PG_USER`, `PG_PASSWORD`
   - `DIRECTUS_URL`, `DIRECTUS_STATIC_TOKEN`
   - (опционально) `DIRECTUS_USERS_COLLECTION`, `DIRECTUS_SHIFTS_COLLECTION`, `DIRECTUS_SHIFT_PHOTOS_COLLECTION`, `DIRECTUS_SHIFT_DEFAULT_STATUS`
2. Установить зависимости:
   ```bash
   npm install
   ```
3. Запустить бота:
   ```bash
   npm start
   ```

При старте бот проверяет подключение к PostgreSQL. Все сообщения пользователю отправляются на русском языке.
